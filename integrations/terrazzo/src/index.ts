import { plugin } from "./plugin";

export { generate } from "./generate";
export { plugin };
export type {
  GenerateOptions,
  GenerateResult,
  PluginOptions,
  ThemeSource,
} from "./types";

export default plugin;
