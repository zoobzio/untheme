/**
 * Shared test fixtures: the valid base theme and switchable catalog from the
 * build stub, whose token shape matches the stub's `#build/types` unions.
 */
export { theme, themes } from "../src/stubs/build/untheme.mjs";
