import m2 from "@untheme/material-2/themes";
import m3 from "@untheme/material-3/themes";
import radix from "@untheme/radix-ui/themes";
import carbon from "@untheme/carbon/themes";

/** Registry of available presets, mapping each preset key to its built-in theme set. */
export const presets = {
  m2,
  m3,
  radix,
  carbon,
};
