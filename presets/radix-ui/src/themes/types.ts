import { defineRadixTheme } from "../preset";

/** A theme produced by `defineRadixTheme` — the shape of every built-in Radix variant. */
export type RadixTheme = ReturnType<typeof defineRadixTheme>;
